/* ============================================================
   SLIDE CONFIG — the single place to control the series.
   Add / remove / reorder slide ids here. Nothing else changes.
   Each id must match a module registered in window.WCSlides.
   ============================================================ */
window.WC_CONFIG = {
  slides: ['s1', 's2', 's3', 's4', 's5', 's7', 's8']
};
